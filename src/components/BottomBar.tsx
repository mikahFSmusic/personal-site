import React from 'react';
import Container from 'react-bootstrap/Container';

function BottomBar() {
    return (
        <Container className="Bottom-bar" fluid>
            Bottom Bar
        </Container>
    );
}

export default BottomBar;